# 文章页自动 TOC（目录）与锚点导航

## 需求 (Spec)
自动为包含多个层级标题的文章生成目录树，并附加平滑滚动锚点。

## 验收标准 (AC)
1. 扫描特定的文章容器（如 `.post-content`）内的 `h2`, `h3` 标签。
2. 生成嵌套的 `ul/li` 目录结构。
3. 点击目录项时，能够平滑滚动到对应的标题位置。

## 边界处理
- 如果没有提取到任何标题，则隐藏目录容器。
- 保证生成的 ID 是唯一的（以防多个同名标题）。
