# Feature: 文章末尾打赏功能 (Post Reward)

## 需求
在文章正文底部提供一个“打赏”按钮，点击后展开显示赞赏码（微信/支付宝），再次点击折叠。

## 验收标准
1. `post.ejs` 中包含打赏区域的 HTML 结构，包含 `id="reward-container"` 和一个 `data-reward-toggle` 的按钮。
2. JS 监听该按钮点击事件，控制 `.reward-qr-codes` 元素的 `is-expanded` class。
3. 单元测试覆盖展开/折叠逻辑。

## 边界
如果页面没有打赏区域，JS 不应报错。
