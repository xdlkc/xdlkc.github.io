# 代码块复制按钮与复制成功提示 (Code Block Copy Button)

## 需求
在每篇文章的代码块右上角自动生成一个“复制”按钮。点击后将代码内容复制到剪贴板，并短暂显示“已复制!”提示。

## 验收标准
- 页面加载后，包含 `<pre>` 的代码块会动态增加 `copy-btn` 按钮。
- 点击时调用 `navigator.clipboard.writeText()`。
- 复制成功后，按钮文字变为“已复制!”，2秒后恢复。

## 边界处理
- 无内容的代码块不报错。
- 剪贴板 API 不可用时（非 HTTPS 等）的降级或静默失败处理。
