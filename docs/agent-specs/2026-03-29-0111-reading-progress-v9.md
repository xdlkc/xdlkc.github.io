# Spec: 阅读进度条 (Reading Progress Bar v9)

## 需求
在文章页顶部添加一个固定宽度的阅读进度条，随页面滚动实时更新。

## 验收标准
1. 当页面滚动时，顶部出现一个 `.progress-bar-v9` 元素。
2. 其宽度百分比应与页面滚动百分比一致。
3. 当滚动到底部时，宽度为 100%。

## 边界
1. 如果页面没有垂直滚动条，进度条宽度为 0。
2. 进度条应在 `DOMContentLoaded` 后初始化。
