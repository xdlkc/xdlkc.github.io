# 代码块复制按钮 (Code Block Copy Button)
## 需求
为文章中所有的 `<pre><code>` 或是高亮代码块添加一个“复制”按钮。点击后复制内容，并提示“已复制”。

## 验收标准
1. 所有代码块右上角出现“Copy”按钮。
2. 点击按钮后代码内容被写入剪贴板。
3. 复制成功后，按钮文字短暂变成“Copied!”。

## 边界
- 仅处理带 `figure.highlight` 或 `<pre>` 元素的代码块。
